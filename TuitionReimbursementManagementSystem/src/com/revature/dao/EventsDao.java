package com.revature.dao;

import org.apache.log4j.Logger;
import com.revature.connector.Connector;
import com.revature.services.EventsServices;

;

public class EventsDao implements EventsServices{

	Connector conn = Connector.getConnector();
	Logger log = Logger.getLogger(EventsDao.class.getName());
	
	
	
	
//				try (Connection con = conn.getConnection()) {	
//								String sql ="SELECT FROM events WHERE (certification, certificationprep, other, seminar, technicaltraining,"
//																	+ "universitycourse) VALUES(?, ?, ?, ?, ?, ?)";
//					con.setAutoCommit(false);
//					
//					PreparedStatement pmpt = con.prepareStatement(sql);

		
		
		
		
		
		
		
		
		
		
		
}
